# Polymarket AI 预测市场监控系统

## 一句话概述
自动抓取 Polymarket 热门预测市场 → 搜索相关新闻 → AI 分析定价偏差 → 推荐操作 → 自动复盘。用新闻信息差找市场定价滞后的低估事件。

---

## 项目结构

```
PMAI系统代码\
├── main.py              # 主入口，调度器。扫描流程 + 定时任务
├── config.py            # 所有配置参数（阈值、模型、API Key）
├── polymarket_client.py # Polymarket Gamma API + CLOB API 客户端
├── news_searcher.py     # 新闻搜索（Serper API，Google新闻接口）
├── claude_analyzer.py   # AI 分析核心（调用 DeepSeek/Claude API）
├── auto_review.py       # 自动复盘模块（检测已结算市场，自动更新交易结果）
├── experience_manager.py # 经验积累（存储/加载历史决策记录）
├── trader.py            # 交易执行（模拟模式 + 真实下单 + 风控）
├── circle_wallet.py     # Circle 开发者钱包（Arc 测试网链上结算）
├── notifier.py          # 通知（Telegram Bot / Email）
├── README.md            # 项目说明（英文）
├── 系统说明.md           # 本文件
├── requirements.txt     # Python 依赖
├── .env                 # API 密钥（不提交到 Git）
├── data/
│   └── experience_log.json  # 所有分析记录（核心数据文件）
└── logs/
    ├── monitor.log      # 运行日志（每次扫描的完整记录）
    └── trade_log.json   # 交易执行记录
```

---

## 完整扫描流程（每次约 35-60 秒）

```
Step 0: 自动复盘
  → 遍历 experience_log 中 result=null 的记录
  → 调 Gamma API 查对应市场是否已结算
  → 已结算 → 判断 WIN/LOSS → 计算 PnL → 自动更新记录

Step 1: 获取市场
  → 调 Gamma API 获取交易量 Top 30 活跃市场
  → 过滤：24h 交易量 < $10,000 的跳过
  → 去重：1 小时内已分析过的市场跳过（突发新闻也能及时捕获）
  → 极端价格过滤：YES > 99% 或 < 1% 跳过（已结算/已成定局）
  → 取前 10 个进入分析队列
  → 带 3 次重试，每次递增等待 5s/10s/15s

Step 2: 搜索新闻
  → 每个市场调 Serper API 搜相关 Google News（英文）
  → 返回标题、摘要、来源、日期、链接
  → 间隔 0.5 秒请求，避免限流

Step 3: AI 批量分析
  → 每个市场拼装 prompt：市场信息 + 新闻 + 历史经验
  → 调 DeepSeek V4 Flash API 分析，返回 JSON
  → JSON 包含：概率判断、置信度、操作建议、理由
  → 间隔 1 秒请求

Step 3.5: 二审复核
  → 对所有 action != SKIP 的市场
  → 重新读完整清算规则（Gamma API 的 description 字段）
  → AI 二次审核：规则是否冲突？边界条件是否遗漏？
  → 三种判定：CONFIRM（确认）/ ADJUST（调整）/ OVERRIDE（推翻）

Step 4: 执行决策
  → 三重过滤：
    1. action == SKIP → 跳过
    2. |概率差| < 12% → 跳过（边缘不足）
    3. 置信度 != HIGH → 跳过（审核不确定）
  → 通过 → 模拟下单（PAPER_TRADE）或真实下单（需配置）
  → 记录到 experience_log.json
  → 生成扫描报告 → 日志输出
```

---

## 核心参数（config.py）

| 参数 | 当前值 | 说明 |
|------|--------|------|
| `ACTIVE_MODEL` | `deepseek-v4-flash` | AI 分析模型。可选 deepseek-v4-pro / claude-opus / claude-sonnet |
| `MONITOR_INTERVAL_MINUTES` | 30 | 持续监控模式下的扫描间隔 |
| `MAX_MARKETS_TO_ANALYZE` | 10 | 每轮最多分析几个市场 |
| `MIN_VOLUME_USD` | 10000 | 24h 交易量门槛（美元） |
| `PROBABILITY_EDGE_THRESHOLD` | 0.12 | 市场定价与 AI 判断差距 > 12% 才操作 |
| `MAX_BET_PER_TRADE_USD` | 50 | 单次最大下注金额 |
| `AUTO_TRADE_ENABLED` | False | False=模拟记录不下单，True=真实交易 |
| `DAILY_BET_LIMIT_USD` | 200 | 单日下注总金额上限 |
| `DAILY_LOSS_LIMIT_USD` | 100 | 单日亏损上限（超过暂停） |
| `CIRCLE_WALLET_ENABLED` | 自动检测 | 配置 CIRCLE_API_KEY 后自动创建 Arc 链上代理钱包 |
| `AI_MAX_TOKENS` | 2000 | AI 最大输出 token |
| `temperature` | 0.3 | AI 分析温度（低=稳定一致） |

---

## 费用估算

| 项目 | 单价 | 每轮消耗 | 每天（48轮） |
|------|------|----------|-------------|
| DeepSeek V4 Flash (输入) | $0.014/M tokens | ~3k tokens × 10 市场 | ~$0.02 |
| DeepSeek V4 Flash (输出) | $0.28/M tokens | ~500 tokens × 10 市场 | ~$0.07 |
| DeepSeek V4 Flash (复审) | $0.28/M tokens | ~300 tokens × 3 市场 | ~$0.01 |
| Serper API | $50/月 2500次 | ~10 次/轮 | ~480 次/天 |

**月度合计：约 $4-5**

---

## 运行方式

```bash
# 安装依赖
pip install -r requirements.txt

# 测试单次运行
python main.py --once

# 启动持续监控（每 30 分钟自动扫描）
python main.py
# 按 Ctrl+C 停止

# 手动复盘某笔交易
python main.py --update-result <record_id> <WIN/LOSS/PUSH> <pnl_usd> <教训>
```

---

## 所需 API Key

| API | 环境变量 | 获取方式 |
|-----|----------|----------|
| DeepSeek | `DEEPSEEK_API_KEY` | platform.deepseek.com |
| Serper（新闻搜索） | `SERPER_API_KEY` | serper.dev |
| Circle 钱包（代理链上金库） | `CIRCLE_API_KEY` + `ENTITY_SECRET` | console.circle.com → SANDBOX |
| Telegram 通知（推荐） | `TELEGRAM_BOT_TOKEN` + `TELEGRAM_CHAT_ID` | @BotFather |
| Polymarket 钱包（仅真实交易） | `POLYMARKET_PRIVATE_KEY` | MetaMask 私钥 |

---

## 数据记录格式（experience_log.json 每条记录）

```json
{
  "id": 1,
  "timestamp": "2026-05-10T10:38:32",
  "market_id": "1919425",
  "market_question": "UFC 328: Sean Strickland vs. Khamzat Chimaev",
  "market_yes_price": 0.195,
  "claude_probability": 0.40,
  "probability_edge": 0.205,
  "claude_reasoning": "市场低估了Strickland的胜率...",
  "recommended_action": "BUY_YES",
  "action_taken": "模拟记录: BUY_YES $50",
  "bet_amount_usd": 0,
  "result": null,
  "pnl_usd": null,
  "lesson": null
}
```

- `result` 为 null 时表示待结算，自动复盘会轮询
- `result` 填写后参与胜率统计
- `market_id` 是自动复盘的关键字段

---

## 关键设计决策

1. **去重缓存（1 小时）**：兼顾节省 API 费用与及时响应突发新闻
2. **极端价格过滤**：YES > 99% 或 < 1% 的市场直接跳过（已结算/已成定局，无分析价值）
3. **二审复核**：初次判断后重新读完整清算规则审核，防止规则陷阱（已证明有效：纠正了网球 walkover 规则、足球 90 分钟限制等）
4. **12% 阈值**：不是回测得出，是数学推算。9% 边缘在中间价位只产生 18% ROI，扣掉滑点仅剩 ~15%，太薄
5. **火焰模型**：deepseek-v4-flash 比 pro 快 3.3 倍、便宜 10 倍、零断连，分析质量对这种 JSON 结构化任务差异不大
6. **Circle 钱包集成**：代理 USDC 金库走 Circle Developer Wallets（Arc 测试网），链上可查可验证
7. **单日风控**：日下注上限 $200 + 日亏损上限 $100，防止失控

---

## 联系方式

- 项目作者：桌面 PMAI 文件夹
- AI 助手：Claude Code（deepseek-v4-pro）
- 运行日志：`logs/monitor.log`
